name: alofi0461
description: "لوحة تحكم ويسبر الإداري"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.2
  font_awesome_flutter: ^10.7.0
  cupertino_icons: ^1.0.8

  # ✅ Firebase
  firebase_core: ^2.24.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
